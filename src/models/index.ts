export interface IItems {
  listing_id: number;
  url?: string;
  MainImage?: string;
  title: string;
  currency_code?: string;
  price?: string;
  quantity: number;
}

// export interface ListingProps {
//   items: IItems
// }
